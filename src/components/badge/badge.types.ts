import { FieldProps } from '../field/field.types';

export default interface BadgeProps {
    label?: string;
    backgroundColor?: string;
    color?: string;
    fontSize?: number;
    borderRadius?: number;
    onClick: any
}
